<% flash.each do |message_type, message| %>
  <div class="alert alert-<%= message_type %> p-6 mb-6 rounded-xl shadow-lg bg-blue-300 text-white font-semibold">
    <%= message %>
  </div>
<% end %>
